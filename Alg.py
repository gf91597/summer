#!/usr/local/bin/python3

from DatabaseOps import DatabaseOps
import time

class Alg(object):
    """every alg need use an tuple list to keep data"""
    def __init__(self, lst)
        self.lst = lst

    def get_common_params(self):
        db = DatabaseOps()
        self.db = db

    def find_last_value(self):
        #find the last value, if the value is high, high,, low high
        #low blue, and the tail is up or down






